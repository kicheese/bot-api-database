{
  "name": "@kicheese/bot-api-database",
  "version": "1.0.0",
  "description": "prisma client for bot-api",
  "main": "index.js",
  "scripts": {
    "db:introspect": "prisma introspect",
    "db:generate": "prisma generate",
    "postinstall": "tsc && cp src/index.d.ts dist/index.d.ts && npx prisma@4.5.0 generate",
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "author": "Sergey Kichigin",
  "license": "ISC",
  "publishConfig": {
    "@kicheese:registry": "https://npm.pkg.github.com"
  },
  "dependencies": {
    "@prisma/client": "4.5.0"
  },
  "devDependencies": {
    "dotenv": "^16.0.3",
    "prisma": "4.5.0",
    "typescript": "^4.8.4"
  }
}
