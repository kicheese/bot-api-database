{
  "name": "bot-api-database",
  "version": "1.0.2",
  "description": "prisma client for bot-api",
  "main": "index.js",
  "scripts": {
    "db:introspect": "prisma introspect",
    "db:generate": "prisma generate",
    "postinstall": "tsc && cp src/index.d.ts dist/index.d.ts && npx prisma@4.5.0 generate",
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/kicheese/bot-api-database"
  },
  "author": "kicheese",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "4.5.0"
  },
  "devDependencies": {
    "dotenv": "^16.0.3",
    "prisma": "4.5.0",
    "typescript": "^4.8.4"
  }
}
